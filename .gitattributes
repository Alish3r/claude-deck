# Shell scripts and Swift sources in the macOS hand-off package MUST stay LF.
# CRLF makes bash on macOS fail with "bad interpreter: no such file or directory",
# which is a baffling first error for an external tester to hit.
langdeck/macos/*.sh    text eol=lf
langdeck/macos/*.swift text eol=lf
